name: TopG 6hr Auto Voter

on:
  schedule:
    # Runs at 10 minutes past every 6th hour
    - cron: '10 */6 * * *'
  workflow_dispatch: # Allows you to test it manually

jobs:
  vote:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        # UPDATED: v4 to v6 to support Node 24
        uses: actions/checkout@v6

      - name: Set up Python
        # UPDATED: v4 to v6 to support Node 24
        uses: actions/setup-python@v6
        with:
          python-version: '3.12'

      - name: Install Playwright
        run: |
          pip install playwright
          playwright install chromium
          playwright install-deps

      - name: Run Vote Script
        run: python vote.py
